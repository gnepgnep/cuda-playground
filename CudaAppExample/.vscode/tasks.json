{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "CUDA build",
			"type": "shell",
			"command": "nvcc",
			"args": [
				"-g",
				"${file}",
				"-o",
				"${fileDirname}/${fileBasenameNoExtension}",
			],
			"options": {
				"cwd": "${fileDirname}"
			},
			"group": "build"
		}
	]
}